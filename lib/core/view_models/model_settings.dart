import 'base_model.dart';

class ModelSettings extends BaseModel {
  // If set to true, checking a habit done will be counted in the backend but not presented to the user.
 bool checkIfMajor = false;
}
